# 手机语音与人民币预算实施记录

## 分支与验证边界

仅追加到 `codex/mobile-voice-stage-link` / 草稿 PR #1；基线 `a5f6d03`。
原工作目录的场景数据库和日志未带入本分支。未合并、未部署、未修改 main。

## P0 首批

- 已安装 OpenAI 7.13 类型支持转写 prompt；新增戏剧词汇，保留音频容器/编码验证。
- 命令输出默认6000、硬上限8000；剧本分块独立12000，支持8000–12000配置及独立模型名。
- 模型上下文最多100个详细对象，选中/名称匹配/入口优先，再按邻近距离取对象；其他对象只有名称、类型和ID。最终写入仍检查完整场景。
- 服务端调用记录明确投影白名单字段，记录真实响应 usage、关联请求ID、延迟及错误类型；不记录原文。尚未收到用量的字段保留 null。
- 合成脱敏文字黄金样例不是录音语料；相关回归102项通过，包括既有配对、音频、原子事务、撤销和空间校验。
- Editor TypeScript通过；8个包构建通过，包含MCP；Next生产构建通过。
- 清除远端锁文件残留的5个已删除插件/依赖条目，严格冻结安装通过。

## 尚未实测

真实 API Key、真实付费模型、导演/演员真实录音、iPhone Safari、弱网和来电中断均未实测。
真实转写准确率、真实模型字段正确率、端到端P50/P95和真实平均账单不可由合成测试推断。

## 尚未完成

P0本地停止/取消/撤销和“删除刚才添加的对象”已接入；删除有独立确认，撤销拒绝跨越后续手动编辑。
P1本机建议/连续制景/方案草台已接入。明确授权后签发HttpOnly会话，按项目与30分钟空闲期限校验；超限、删除、舞台尺寸调整退回确认。草台不写节点，最终一次编译提交。相关回归109项通过，类型检查与生产构建通过。
用户已明确选择先完成本机版，公网授权保持禁用。当前无账号/项目拥有者体系，不能将本机工作区身份宣传为公网用户认证。
P1高级组合、资产替换、可配置空间约束及手机执行回执仍待扩展。
P2持久费用账本、预算拦截及生产共享存储尚待实现。
