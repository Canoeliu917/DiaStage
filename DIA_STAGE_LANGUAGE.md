# Dia 舞台语言与 Beta 接线状态

2026-09-13。本轮总体状态：**PARTIAL**。Product Backbone 固定为 Venue → Build → Rehearse → Remount；Version、History、Journal、Feedback、Human Authority、Scene Store 和 Layers 保持现有职责。

## 事实来源

| 内容 | 事实来源与当前状态 |
| --- | --- |
| 22 项置景的 canonical ID、名称、尺寸、图例 | 用户提供的《DiaStage_置景图例与操作指南》整合版 1.0，第 4 节；随后提供的 `DiaStage_Prop_Menu_Preview/prop-menu-manifest.json` 为实际资源映射与当前模型事实来源。对应 `packages/core/src/stage/object-registry.ts`，原图确认尺寸与模型补充尺寸分别保留。 |
| 二帘、三帘整体包围尺寸 | manifest 给出的默认90°姿态：二帘宽0.92、高2.4、深0.92米；三帘宽0.92、高2.4、深0.94米。登记在 `modelDimensions`；打开角度变化后按实例真实网格重新计算包围尺寸及中心，不继续套用默认外框。单片宽0.90米仍独立保留，`defaultDimensions=null` 表示Word未给整体名义规格，不再表示缺模型。 |
| 22 项实际网格、枢轴、原始材质 | 22个GLB与44个PNG已按原字节复制至 `apps/editor/public/stage-library`，保留稳定ID与原始模型结构。22个GLB的SHA-256均匹配manifest；新版包的GLB/PNG与上一包逐字节一致。手动折叠只针对补充说明中的二联、三联；未扩展抽屉动画或门窗部件控制。 |
| 资源包 README、catalog、manifest | 已完整阅读 `README_给Codex.md` 与 `prop-menu-manifest.json`；后者作为本包22项资源目录，不另外虚构catalog。两份原文件随资源保留，包括来源页、`assumed_fields` 与原资料的版权标注。门叶厚度以最新manifest的0.035米为准，Word旧值0.04米注明来源差异。 |
| Blender 导出约定 | 模型实际米制1:1，GLB已为Y向上，不再转换坐标或乘40。只有manifest的Blender Z向上包围数据映射为 `[x,z,-y]` 元数据。资产 `offset/rotation=[0,0,0]`、`scale=[1,1,1]`；`boundsCenter` 表达非中心包围盒，不改模型原点。 |
| 键位和应用帮助 | 上述 Word 第 5–9 节为目标规范，名称固定为“UE5 主布局＋中键环绕”。文档中的“分支已实现”不等于本地全部实现。 |
| 景片折叠 | 已完整阅读 `CODEX_景片折叠接入说明.md` 和新版包 README；最新manifest的 `articulation` 是两个折叠资源的控制事实来源。GLB的实际局部轴为+Y，角度使用相邻板片夹角，180°展开，90°直角。 |

对象的新建解析使用规范别名和尺寸来源；泛称桌子、椅子、门、窗、台块等有多个规范候选时先澄清。修改已有对象时，明确名称须匹配唯一对象；“它/这个”等代词引用唯一选中对象；种类别名仍需得到唯一候选，不按数组首项猜对象。

`prop-assets.ts` 导出同一份22项已安装资源列表，菜单、Dia和服务端口令入口共同使用。`ground-assets.ts` 仍检查资源可用性；未知或不可用规范模型返回问题并移除待新建物品，不生成替代代理。已有对象可单独调整；同一句话包含澄清问题时，Dia先澄清，不自动执行其中一部分。新建默认使用模型实际包围尺寸，确保实例缩放为 `[1,1,1]`；开着的门叶所形成的外框不会被压成门框名义厚度0.08米。待放置轮廓仅作参考，正式物件使用对应GLB；没有预加载全部22个模型。

标准模型新建与已有物件整体缩放分开处理。若新建方案明确要求不同尺寸（例如直径1.2米圆桌，而标准模型直径0.9米），资源接线会显示两组尺寸并要求澄清，移除该待新建物品，避免静默覆盖精确数值或自动缩放。用户可确认标准尺寸后添加，再通过现有整体缩放调整；不会因此改写资源文件。

## 已接线的语义

| 能力 | 例子 | 路径与限制 |
| --- | --- | --- |
| 舞台方向、精确移动 | 把桌子往台前移动30厘米；门向台右移100毫米 | 解析 → 当前对象 ID → 米制量 → 确定性 StagePlan → validator → Ghost → 用户采用。30厘米保持0.3米，毫米不会混成厘米。 |
| 小幅移动 | 桌子往台右一点；圆桌往台左稍微挪一点 | `small` 语义。距离取物品在该方向尺寸的25%和场地跨度的2%中较小值，限定0.05–1米，预演说明假设。精确数值不走此规则。 |
| 已有布景旋转、中心落位 | 椅子旋转45度；把圆桌放在台中 | 复用现有布景方案与确认流程。语言旋转目前只接绕竖直轴；不声称任意轴、支点编辑已完成。 |
| 人物相对移动 | B离A远一点；A别动，只让B靠近 | 复用现有 `withdraw / approach / hold` suggestions 和排演编译器；未唯一确定人物或参照时澄清。保持的人物不移动；没有自动采用。 |
| 人物与物件站位 | A站在门旁边 | 复用已有 `stand-near-object`。多段路线、绕障、复杂朝向仍需要现有手动工具或明确方案；不虚构执行成功。 |
| 版本和复台 | 查看已保存版本；将指定版本用于另一场地 | 保留当前版本/复台面板和预览入口；恢复或复台确认不由语言自动代签。 |
| View State | 视角往左转一点；从正面看一下；从台左侧面看看；视角低一点；把镜头拉远 | `ORBIT_VIEW / PAN_VIEW / ZOOM_VIEW / SET_VIEW` 只调用观察控制；不生成 Stage transaction。 |
| 聚焦、总览、保存/召回观察位 | 聚焦选中的物品；重置视角；保存当前视角为全景；召回视角全景 | `FRAME_SELECTION / RESET_VIEW / SAVE_VIEW / RECALL_CAMERA`。聚焦需唯一选中；保存名需明确。视角按 sceneId 在本机独立保存，不写 site metadata、正式 Scene 或 Journal。 |

View 命令使用现有 camera pose 订阅和 camera-controls 事件。有效观察命令保留当前等待采用的 Ghost 和方案；第一人称或截图状态中要求先退出相应模式，避免报告未发生的位姿变化。

无对象/方向/幅度的“转一下”“那个挪过去”“这里松一点”“照昨天那样”“弄得更有戏一点”先澄清。未指定幅度的“把门往台右移动”也需补充“一点”或确切距离。新建的多个规范尺寸不能用一个泛称隐式选定。

受控语言尚未覆盖全部 REMOVE / REPLACE / FACE_PERFORMER / 路线编辑句式；这些仍使用现有人工入口或现有提案流程。本轮没有新增这些产品能力。复杂模型请求仍经过严格输出 schema 和 validator；模型不能生成真实碰撞网格、私自指定资产 URL 或伪造证据。

## Beta 能力边界

共享开关位于 `apps/editor/lib/beta-capabilities.ts`：

- `BETA_EXPERT_MEDIA_ENABLED = false`：视频、动画、时间线、关键帧、连续运镜和镜头序列制作入口关闭；已接入的录制、视频导出、关键帧等语言表达会明确说明未开放，不宣称覆盖所有同义句式。
- `BETA_LIGHTING_PRODUCTION_ENABLED = false`：灯光生产功能关闭。观察场景所需基础照明继续由既有渲染负责。
- Camera 保留观察、固定机位、保存和召回。原始镜头数据保留；Beta 不播放其中的物体 motion 或动画。
- 未增加新 workspace、RAG、训练、Auto-RL、产品多代理或全套 UE5 编辑器。二联、三联的可见折叠控制是用户随后提供的专用接入补充，不开放动画制作。

识别到“不对”“不是”“纠正”“我说的不是”“我是想”等纠正开头、且有上一条输入时，纠正记录保存在本机 `diastage:grounding-corrections:v1:<sceneId>`，包含 `UserInput / DiaInterpretation / HumanCorrection / CorrectInterpretation / SceneContext`。当前 `DiaInterpretation` 保存上一条 Dia 回复；`CorrectInterpretation` 保存本轮可解析结果，未解析时为 `null`，或为需要澄清的结果，不补猜目标。这是有界的本地记录入口，不代表所有纠正句式已覆盖。记录明确 `privateProjectData=true`、`trainingAuthorized=false`、`trainingEligible=false`；不会发送为训练样本。

## UE5 目标映射与待适配

| 指南目标 | 当前范围 |
| --- | --- |
| 中键环绕；Shift/Alt＋中键平移；滚轮推近拉远 | 随 Design Foundation 接入现有相机控制，本地浏览器已验证环绕中心、平移方向、滚轮缩放与失焦取消；物件数据不变。专用二维平面图维持平面交互。没有合并其他分支。 |
| 原指南 Q 选择，W 移动，E 旋转，R 缩放 | 用户随后明确要求 WASD 移动观察视角、QE 平行升降，覆盖原 Q/W/E 工具键；W/S 沿观察方向在舞台水平面前后，A/D 横移，Q 下降、E 上升。旋转、缩放保留带图标与字幕的可见工具入口，R 打开缩放属性；本轮不另编旋转字母键或宣称完整三轴 gizmo。输入框、弹窗和失焦不触发视角移动。 |
| F 聚焦 | 舞台模式已接现有相机聚焦：唯一选中时执行，无选中或多选时不猜目标；输入框、弹窗和第一人称优先。`native-focus-check` 浏览器验证通过，正式场景写入为0。右键飞行中的 F 导航含义尚未整套接线。 |
| 右键环视＋WASD；Q/E 世界升降；R/F 相机上下；滚轮速度；Z/C 视野 | 本轮按用户确认接入普通舞台观察的 WASD/QE；不要求先按住右键。完整右键飞行、R/F 导航、滚轮速度与 Z/C 视野组合仍未作为一套 UE5 飞行控制接入。 |
| World/Local、临时/保存支点、分轴缩放、Alt 拖轴复制、End、V 顶点吸附 | 指南目标保留；本轮不宣称完整实现。现有整体物品变换继续使用。 |
| 位置/旋转/缩放吸附各自开关和增量 | 左下“场景选项”与舞台库共用位置网格和景片边缘贴合；默认10厘米位置网格，可单独关闭网格或边缘贴合，两项全关即自由落位。近邻景片按实际模型投影边缘接触，不留预设缝隙；尚未加载模型时使用已知轮廓。整件旋转保留现有步幅，折叠手柄15°；独立缩放吸附仍未完整接入。 |
| Ctrl+W 等浏览器保留键 | 保留指南键位与可见菜单入口要求；无法拦截时标“待适配”，不静默改键。 |

按指南第 6 节方块练习修正了现有整件旋转：固定现有几何中心，以当前坐标体系的四元数组合旋转，删除为了维持最低点高度而自动抬高的处理。1米方块中心高0.5米，单轴倾斜30度后中心仍为0.5米，最低角允许到约-0.183米。落地高度需要用户另行调整；本轮未扩展通用 End、支点或 UE5 变换工具。模型资产、缩放、部件相互关系和一步撤销保留。

自由置景的 stage policy 不再因鼠标扫过桌面或台块面而自动挂接或抬高，保持当前地面自由放置和碰撞提示。明确声明 `attachTo` 的建筑附着物继续使用原有附着路径；本轮不扩展父节点坐标骨架。

## 二联与三联折叠补充

实例的 `controls.fold_angle_1_deg` / `fold_angle_2_deg` 独立保存，缺失值才取90°。仅调整原模型 `Hinge_02` / `Hinge_03` 的绝对局部角度，不改整体position、rotation、scale。各实例使用独立节点树，共享静态网格；两个折叠资源不参与跨节点静态合批。正常选择和移动仍作用于整件。

“折叠景片”入口位于选中对象属性面板。“折叠位置”和“打开角度”支持可见控制柄、数值及90°/180°/270°预设。用户最新明确将角度上限改为270°，覆盖素材说明中的原180°限制；原素材文件保持不变。三维、鸟瞰和Dia平面图的外端或拐点使用同一15°步进逻辑。连续拖动使用已有live overrides，松手同一事务写入controls与动态包围尺寸；取消清除预览。固定锁同样阻止折叠。

自碰限制针对这两款模型的真实板体和框杆，按局部凸轮廓与高度区间保守检测沿角度路径的穿插，排除相邻连接立框和连接销的正常接触。整件落位仍允许与其他道具重叠并显示红色。180°以上可反折至270°，实际板体自穿插仍按现有检查限幅；这不是五金实物限位或任意关节物理模拟。0°仍为数学端点，不保证实物无穿插闭合。

选中布景提供“形状初始化”：22款标准资产恢复原始尺寸、零转向/倾斜、单位缩放，折叠景片恢复90°；保留位置、名称和身份，支持撤销并遵守固定锁。未知自定义块仅恢复现有旋转，不推测原始拓扑。普通抓取不更改尺寸；精确尺寸字段仍放在可展开的高级区域。精确位置以整数厘米显示，未编辑的聚焦/失焦不将原有高精度坐标取整存回。

三维碰撞、小图轮廓和分屏间距读取当前已加载模型；折叠与整件旋转后刷新。平面图按模型各连通部件投影，圆桌不再套用矩形轮廓；分屏间距取实际投影部件之间的最近距离。目标浏览器交互和最终构建结果以本轮验收报告为准。

## 验证口径

`stage-command-eval.ts` 保存 **40 条正常语义样例、15 条澄清/拒绝样例**，每项包含 input、expectedIntent、expectedCapability、expectedTarget、expectedDirection、expectedAmount、clarificationRequired。`stage-command-eval.test.ts` 执行真实 parser、grounding、人物规则与编译器，不调用真实模型。

40 条正常样例保持为20项名称解析、8条布景操作、10条观察命令和2条人物关系命令。资源接入回归另行覆盖全部22项的语言/服务端新建、共享资产ID、米制原始缩放、实例ID与变换独立性，包括二帘、三帘。缺资源拒绝仍通过显式空可用列表回归，不将缺资源场景改写为成功。

另有回归覆盖：精确长度不被 small 替代、场地尺度影响 small、同种/同名对象澄清、缺资产不产生代理、视角保存隔离场景、固定视角幂等、View 命令保留 Ghost 且正式 Scene commits 为零、只在本机记录纠正。

本轮语言样例 **55/55 PASS**。真实语言模型服务评测：**NOT_RUN**。真实手机/远程设备评测：**NOT_RUN**。这里的模型服务评测指在线LLM，并非GLB文件核验；完整源码与浏览器最终验证由本轮验收报告记录，本文不以离线样例替代设备验证。

素材包与折叠接入后的集中回归为 **417 PASS / 0 FAIL（64个文件、14950次断言）**；界面专项另跑 **27 PASS / 0 FAIL（9个文件）**。包括22项实际新建、文件哈希/PNG规格、非中心轮廓、明确尺寸冲突澄清、折叠独立实例/自碰限幅/历史恢复、碰撞共享缓存和固定后的预览清理。此结果包含原有55条语言样例，不能将离线解析通过解释为在线LLM或UE5完整键位验收。最终构建和本地浏览器结果以验收报告为准。

## 2026-09-13 最新操作覆盖

- 默认按工具栏“每格”尺寸落位；三维、平面、分屏和Dia采用同一格距。Ctrl轻按并松开切换格距。自由放置位于左上道具栏末尾，属于特殊选项；贴边独立开启时可优先离开网格贴合。
- 点击旋转后按住右键左右拖动，松开确定，Esc取消；WASD继续负责观察导航，Q下降、E上升。平面与分屏允许右键转动同时按WASD，指南针居中归正。
- 所选二帘/三帘的全部链条端点显示圆节点；前两个节点用于整件落位，其余节点用于15度折叠调整，不缩放板片。
- 左侧场地/资产/属性在上，舞台总览在独立可调下半区。记录显示最近20轮，收藏独立保留；采用权仍归用户。
