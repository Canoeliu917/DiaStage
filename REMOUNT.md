# 复台 Remount

复台把排练场的物件布置映射到目标舞台。它使用明确的测量点与坐标计算，不能代替现场复核。

## 当前可用流程

顶部 **复台**打开六步映射面板。新舞台源场尺寸直接来自实际舞台定义，已有复台配置优先保留。布景和 Camera Studio 机位可共同选择；机位通过元数据适配，不创建伪造场景节点。

1. 记录源场尺寸与本场物件，冻结源位置，不移动正式节点。
2. 录入目标场地宽深高，以中心线和台口线为基准，保持 1:1。
3. 输入两套对应的三个不共线水平基准点，检查校准误差和安全净距。点序与坐标方向必须一致。
4. 生成幽灵预览，检查源/目标坐标、旋转、基准距离与冲突原因。尺寸不足时报告冲突，不缩小实体。
5. 核对并确认复台，把可支持的物件位置、旋转与复台配置作为一个批量操作提交；场景继续自动保存。
6. 使用撤销恢复整批操作。数字落位记录不代表现场物件已验收。

灰色为目标参考，蓝色为源位置，绿色为可落位，黄色为净距提示，红色为冲突。颜色同时配有文字原因。

## 保存与预览边界

配置保存在 `site.metadata.remount`，版本为 1，保留根节点其他 metadata。保存配置或确认应用前的面板草稿不改变正式节点。

只通过 scene store 的 `applyNodeChanges` 提交，不直接写数据库。配置被其他编辑或撤销改变时，过期草稿必须重新载入。幽灵预览只属于展示层，采用独立叠加层，不参与拾取。

- `packages/core/src/remount/`：纯 TypeScript 数据、三点校准、刚体映射、包围体碰撞和净距计算。
- `apps/editor/lib/remount-scene.ts`：兼容层级变换的场景适配与批量更新。
- `apps/editor/components/remount-panel.tsx`：草稿、确认与验收界面。
- `apps/editor/components/remount-preview-system.tsx`：只读幽灵显示。

旧层级和不支持的挂接构件保留读取。无法可靠转换的对象必须列出原因，不应静默移动。

## 数学与几何限制

当前只执行水平舞台的刚体 1:1 映射。非 1:1 模式虽然保留数据枚举，执行入口拒绝缩放。三点计算不是多点最小二乘测量，容差不能解释为测绘精度。

冲突检查基于声明尺寸的保守包围体，复杂形状可能误报。扫描参考不参与精确网格碰撞。历史手工走位折线能映射，但演员时序路径、多人碰撞、观众视线、通道与净空完整分析仍需后续独立实现。

## 三入口与复台

手动、语音、文字和剧本形成的布景使用同一场景节点，均可进入复台。摄影机关键帧位置、注视点与跟随偏移共同映射；有独立运动或未选跟随主体时明确阻止应用。人物模拟路线不作为实体设备。六步流程保持不变，候选排除旧灯具。未测量的净高不能用渲染默认 4 米冒充，预览和确认都会要求补测。

`VenueCaptureAdapter.loadVenueProfile(source: unknown): Promise<VenueProfile>` 仅为采集接口边界，未接入真实摄像头、LiDAR、摄影测量、AR 或重建服务。

## 验证记录

重构前的数学、保存、撤销与浏览器验证全文保存在 [历史 REMOUNT.md](docs/history/2026-09-10-before-theatre/REMOUNT.md)。本轮验证必须重新运行，不能复用旧日期的通过结论。
