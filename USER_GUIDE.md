# DiaStage 用户指南：UE5 标准置景操作

本指南统一采用 **Windows + 普通三键鼠标、UE5.6 默认关卡编辑器**的操作规格，覆盖舞台观察、道具变换、支点、吸附、放置、多选和编组。先前提出的 Q/E 左右环视和 Ctrl 旋转要求已撤销，不设为默认操作或另加习惯预设。

**适配状态：这是标准操作指南及开发目标。本次同步文档与应用内帮助，尚未修改运行时键位。** 当前 DiaStage 与此标准的差异见文末，不能据此认为全部操作已经生效。

## 操作前先分清对象

| 对象 | 改变什么 | 方块例子 |
| --- | --- | --- |
| 编辑视角 | 相机位置或视线 | 走近方块、观察背面 |
| Location | 道具位置 | 移到台口 |
| Rotation | 绕支点改变姿态 | 水平转向或倾斜 |
| Scale | 尺寸倍率 | 正方体拉成长方体 |
| 内部部件 | 子部件相对父物体的变换 | 门扇绕铰链开启 |
| 动画 | 变换随时间变化 | 方块反复来回摆动 |

“+”表示同时按住；“W / S”表示两种按键选择。Q/W/E/R 是工具选择键，需随后拖动控制柄或输入数值，物体才发生变换。**右键飞行状态优先使用导航键**：右键按住时 E 是相机上升，松开右键后 E 才是旋转工具。

## 工具与物体变换

| 操作 | UE5 标准操作 | 使用说明 |
| --- | --- | --- |
| 选择 | Q；左键单击物体 | 选择场景中的道具实例。 |
| 移动 | W，再用左键拖动移动轴 | 拖箭头约束单轴；拖平面控制柄约束双轴。 |
| 旋转 | E，再用左键拖动旋转圆弧 | 绕选定轴和支点旋转；单按 E 只切换工具。 |
| 缩放 | R，再用左键拖动缩放柄 | 单轴拉伸；中心控制柄等比缩放。 |
| 切换变换工具 | 空格 | 循环移动、旋转、缩放。 |
| 精确变换 | Details → Transform 数值输入 | Location 是位置；Rotation 是角度；Scale 是倍率。 |
| 世界 / 局部坐标 | Ctrl + 反引号（`），或工具栏坐标图标 | 世界轴固定，局部轴随物体朝向变化。 |
| 临时移动支点 | 中键拖动变换器的支点 | 改变旋转中心；临时偏移不等于修改网格原点。 |
| 保存 / 恢复支点偏移 | 右键菜单 → Pivot | 使用 Set as Pivot Offset / Reset Pivot Offset。 |

依据 [Epic：物体变换](https://dev.epicgames.com/documentation/en-us/unreal-engine/transforming-actors-in-unreal-engine?application_version=5.6)及[工具栏](https://dev.epicgames.com/documentation/en-us/unreal-engine/viewport-toolbar?application_version=5.6)、[坐标与支点](https://dev.epicgames.com/documentation/en-us/unreal-engine/coordinate-system-and-spaces-in-unreal-engine?application_version=5.6)。

## 透视视口：观察与飞行

| 操作 | UE5 标准操作 | 使用说明 |
| --- | --- | --- |
| 原地环视 | 按住右键，移动鼠标 | 改变视线方向；道具不动。 |
| 前后 / 左右飞行 | 按住右键 + W / S；A / D | W 前进，S 后退，A 左移，D 右移。 |
| 沿世界竖直轴升降 | 按住右键 + Q / E | Q 下降，E 上升。 |
| 沿相机局部上下轴移动 | 按住右键 + R / F | R 局部向上，F 局部向下；视角倾斜时与世界升降不同。 |
| 飞行速度 | 按住右键 + 滚轮 | 调整移动速度。 |
| 视野范围 | 按住右键 + Z / C | Z 增大视野角度，C 减小；不是缩放道具。 |
| 鼠标前后移动 / 转向 | 左键拖动空白视口 | 前后移动并左右转向；命中控制柄时优先操作控制柄。 |
| 平移画面 | 中键拖动，或同时按左右键拖动 | 观察位置上下左右移动，视线方向保持。 |
| 推近 / 拉远 | 滚轮 | 逐步前后移动观察位置。 |
| 聚焦选择 | F | 将选中物体纳入观察范围。 |
| 围绕焦点观察 | Alt + 左键拖动 | 可先按 F 聚焦，再绕焦点观察。 |
| 围绕焦点推拉 / 平移 | Alt + 右键 / Alt + 中键拖动 | 分别推拉、平移观察视角。 |

依据 [Epic：视口键鼠操作](https://dev.epicgames.com/documentation/en-us/unreal-engine/viewport-controls-in-unreal-engine?application_version=5.6)。

## 选择、正交视图与显示

| 操作 | UE5 标准操作 | 使用说明 |
| --- | --- | --- |
| 追加选择 | Ctrl 或 Shift + 左键单击 | 加入当前选择；与拖动物体的变换操作区分。 |
| 正交框选 | 左键拖出选框 | 替换当前选择；Shift + 左键框选追加。 |
| 正交框选移除 | Ctrl + 右键拖出选框 | 从当前选择中移除框内物体。 |
| 正交观察 | 右键拖动；滚轮 | 分别平移、缩放平面视图。 |
| 切换透视 / 正交 | 视口 Camera 菜单 | 选顶、底、左、右、前、后等观察方向。 |
| 游戏视图 | G | 隐藏编辑辅助显示；不等于开始排演或录制。 |
| 沉浸视口 | F11 | 扩大视口显示。 |
| 实时视口 | Ctrl + R | 切换视口实时更新；不等于保存或重做。 |

依据 [Epic：视口键鼠操作](https://dev.epicgames.com/documentation/en-us/unreal-engine/viewport-controls-in-unreal-engine?application_version=5.6)。

## 放置、对齐与吸附

| 操作 | UE5 标准操作 | 使用说明 |
| --- | --- | --- |
| 添加道具 | 从内容浏览器拖入视口 | 创建独立实例，再调整位置与姿态。 |
| 位置 / 旋转 / 缩放吸附 | 工具栏分别开启并设置增量 | 三种吸附独立；连续旋转需关闭旋转吸附。 |
| 贴到表面 | End | 将选中道具贴到最近的可吸附表面；检查目标是否为预期地面。 |
| 拖动贴面 | 开启 Surface Snapping | Surface Offset 设置与表面的偏移距离。 |
| 随表面倾斜 | 开启 Rotate to Surface Normal | 贴面时按法线调整朝向；关闭时保持原朝向。 |
| 顶点吸附 | 使用 W 移动工具时按住 V | 以几何顶点进行对齐。 |
| 复制并移动 | Alt + 拖动移动轴 | 生成新实例，并摆到新位置。 |

依据 [Epic：吸附与贴面](https://dev.epicgames.com/documentation/en-us/unreal-engine/actor-snapping-in-unreal-engine?application_version=5.6)。

## 编组与批量变换

| 操作 | UE5 标准操作 | 使用说明 |
| --- | --- | --- |
| 编组 / 重新编组 | Ctrl + G | 所选道具按组共同变换。 |
| 取消编组 | Shift + G | 解除组关系，保留道具。 |
| 切换组选择 | Ctrl + Shift + G | 切换 Allow Group Selection。 |
| 锁定 / 解锁组 | 右键菜单 → Group → Lock / Unlock | 解锁后可调整成员；锁定时整体变换。 |

依据 [Epic：编组](https://dev.epicgames.com/documentation/en-us/unreal-engine/grouping-actors-in-unreal-engine?application_version=5.6)。

## 常用编辑与界面范围

复制、剪切、粘贴、删除、撤销、重做和保存从编辑菜单进入；快捷键以目标版本 Editor Preferences → Keyboard Shortcuts 中对应关卡编辑器命令为准。新增实现必须同步这些命令及其提示，不能沿用现有 DiaStage 的冲突映射而标称完全符合 UE5。

这里只讲关卡视口内的置景操作。网格建模、材质、蓝图和时间线各有自己的命令上下文，不能将其中的快捷键直接当作通用道具操作。网页端的 F11、Ctrl+R 等可能由浏览器处理；在 DiaStage 完成适配和实测前，应使用可见的界面入口，并标出这一差异。

## 方块练习：移动、转向、倾斜、摆放

设方块边长 **100 cm（1 m）**、支点在中心，地面高度为 Z=0。给一面加“正面”标记，避免对称形状掩盖旋转结果。以下是 UE5 标准操作练习。

1. **放到地面。** 未旋转时将中心高度设为 Z=50 cm；位置值表示支点，而非最低点。也可以从上方按 End 贴面。
2. **平移。** 按 W，拖红色 X 轴、绿色 Y 轴或蓝色 Z 轴。世界坐标中 X 前后、Y 左右、Z 上下；拖 XY 平面控制柄在地面平面内移动。
3. **水平转向。** 按 E，在世界坐标下拖 Z 轴圆弧，或在 Details 的 Rotation 中将 Z / Yaw 设为 45°，另外两个分量保持 0。
4. **倾斜。** 先复原旋转，再把 Y / Pitch 设为 30°；绕 X 的分量是 Roll。先分别练习三根轴，再组合姿态，屏幕左右不能代替世界轴方向。
5. **从底边翻倒。** 临时把支点移到底边，再转动方块。绕中心倾斜时底角可能穿入地面，需要重新调高度或贴地。移动支点与移动网格是不同操作。
6. **缩放。** 按 R，拖中心控制柄等比缩放；拖单轴改变一个方向的大小。Scale 是倍率，100 cm 方块的 X 倍率为 2 时，该方向长度成为 200 cm。
7. **复制排列。** 按 W，Alt+拖动移动轴生成第二个方块；开启位置吸附并选择如 10 cm 的步长。这个数值是练习选择，不代表固定默认值。
8. **编组与检查。** 多选两块，Ctrl+G 编组；W/E/R 对整组操作。Shift+G 取消编组。F 聚焦后 Alt+左键环绕检查，确认高度、穿插和通道。

三轴语义、颜色和坐标空间依据 [Epic 坐标系统](https://dev.epicgames.com/documentation/en-us/unreal-engine/coordinate-system-and-spaces-in-unreal-engine?application_version=5.6)；变换输入依据 [Epic 物体变换](https://dev.epicgames.com/documentation/en-us/unreal-engine/transforming-actors-in-unreal-engine?application_version=5.6)。

## 世界轴、局部轴、支点与单位

UE5 使用左手坐标系，X 向前、Y 向右、Z 向上；默认长度单位为厘米，角度为度。世界轴不随道具旋转，局部轴随道具朝向改变。沿世界“上”移动与沿倾斜道具的局部“上”移动，会得到不同轨迹。

本次置景资产以米建模。底面中心适合家具整体定位，门扇使用独立铰链，折叠景片逐片保留铰链。整个道具的变换和内部部件开合须分别操作。资产默认支点不一定在几何中心，所以不能把方块练习中的 50 cm 高度套用于所有模型。

## 静态倾斜与持续摆动

“摆成某个角度”只保存一个姿态；持续摆动需要动画或排演时间线。例如依次设为 0 秒 -15°、0.5 秒 +15°、1 秒 -15°，再设循环，这是自行定义的动画例子。E 只选择旋转工具，不会自动创建这样的动作。道具开合也应使用部件属性或时间线，不占用 E、R 等通用工具键。

吸附解决空间对齐。贴面和法线对齐分别控制位置与朝向，不能把一次贴面当作已经核对承重、稳定或真实通行。

## 当前 DiaStage 的适配差异

以下是源码基线 `4170d1919a8eb25a7bc96503dfb55098b71bbbfe` 的迁移清单；它记录现状，不作为保留旧习惯的要求。

| 现有行为 | UE5 标准目标 / 所需适配 |
| --- | --- |
| V 选择，E 操作门窗，R/T 步进旋转 | 统一 Q 选择、W 移动、E 旋转、R 缩放；门窗开合回到部件入口 |
| Ctrl+右键旋转；Ctrl+Shift+右键自由旋转 | 以 E 旋转操纵器及旋转吸附开关替代，不保留为默认旋转手势 |
| 普通视图 WASD 平移、右键环绕 | 透视右键环视并启用 WASD 飞行；Alt+左键环绕 |
| 无人机模式 Q/E 升降 | 统一到右键飞行情境：Q 下降、E 上升 |
| F 切换布置图层；空格配左键平移 | F 聚焦；空格切换变换工具；图层用现有可见入口 |
| 轻按 Ctrl 切换网格步长 | 位置、旋转、缩放吸附分别使用工具栏设置 |
| Ctrl+Shift+G 取消编组；组只在会话有效 | Shift+G 取消编组，Ctrl+Shift+G 切换组选择；补齐组数据保存 |
| 普通直接旋转主要改变水平朝向 | 通用物件补齐三轴移动、旋转、缩放以及世界 / 局部空间 |
| 当前底层为 Three.js 的 Y 向上、米制 | 界面按 UE5 坐标、单位解释；导入与保存经明确转换，避免破坏旧场景 |
| End 贴面、V 顶点吸附、临时支点未统一接入 | 实现后逐项验证；暂未完成的功能明确标注 |
| 浏览器和不同编辑上下文占用键位 | 核对焦点、鼠标状态和浏览器限制，逐项报告可用性 |

实施细则见 [Codex 交接说明](docs/UE5_CONTROLS_IMPLEMENTATION.md)。需同时更新二维、三维视图、工具栏、鼠标提示、用户指南和历史保存。当前文档提交不改变运行时行为。

## 置景资产来源

《景图示意图总集(1).ppt》整理为 6 类、22 个排演资产，含空间围合、门窗、台块与支撑、沙发、桌、椅凳。原图明确尺寸与建模假定分开显示：一号台块的 1.80×0.90×0.15 m 有来源；圆桌只确认直径 0.90 m，高度需实测。示意比例 1:40 已转换为实际尺寸，模型不得再次缩放 40 倍。
