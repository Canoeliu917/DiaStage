# 镜场 Studio · 本地 Pascal 版

本目录包含 Pascal 开源编辑器的完整源码，并在原生编辑界面里加入机位、连续运镜、目标跟随与无声录像。原目录 `D:/Codex/editor` 未修改。

本地入口：<http://127.0.0.1:4318/scene/empty-table-camera>。

## 启动

需要 Node.js 22.13 以上，建议 Node.js 24；`node` 和 `npm` 须可在终端运行。双击同目录的 **启动镜场.cmd**。窗口会安装缺失依赖、构建内部包，然后在 `127.0.0.1:4318` 启动开发服务。首次安装需要联网。Bun 固定为 1.3.14，通过 npm 临时调用，不要求全局安装。

保留启动窗口；结束服务时按 Ctrl+C。本启动方式只监听本机。

第一次在新目录使用时，等终端显示 Next.js 已就绪，再在本目录打开另一个终端执行：

```powershell
npm exec --yes --package=bun@1.3.14 -- bun scripts/seed-camera-scene.ts
```

脚本从 `examples/empty-table.json` 创建固定 ID 为 `empty-table-camera` 的样例。只有查询返回不存在时才创建；已有场景不会被重置。样例基于原生场景 `S00_BASE.json`，共 33 个节点；房间已调整为米白哑光墙面、浅暖木色地板与门扇，搭配“阴天”柔和环境光。

仅检查启动条件、不开服务：

```powershell
.\启动镜场.ps1 -Check
```

种子脚本 `--check` 只查询当前服务；`--self-test` 在内存中验证创建条件，不访问服务。

## 试拍一个镜头

1. 打开样例，点击顶部 **镜头** 进入镜头面板。样例包含「全景缓推」「绕桌侧拍」「跟随行动线」三个机位。
2. 选一个机位，点击播放，看摄像机在关键帧间连续移动。拖动时间条可以检查某一时刻；停止后还原预演前的位置。
3. 调整编辑器视角，点击 **保存当前机位**。可编辑关键帧的时间、摄像机位置、注视点、视场角，也可新增机位。
4. 在跟随设置中选择场景物体：沿路径移动并看向目标，或与目标保持固定距离。需要目标移动时，为它设置行动线。
5. 底部选择横屏 720p、横屏 1080p 或竖屏 1080p，然后点击录制。录制当前机位；结束后可在页面回放，并通过下载入口保存文件。

录像只有三维画面，没有配音、麦克风或系统声音，也不捕获桌面与编辑面板。浏览器支持 MP4 时输出 MP4，否则输出 WebM，文件后缀与实际格式一致。输出采用 30 fps 实时录制，画幅不匹配时加黑边保全画面，不裁掉场景；不是离线逐帧渲染。设备性能与浏览器会影响实际流畅度。

## 保存与可编辑范围

场景继续使用 Pascal 原生保存与导入功能，以及 `/api/scenes`、`/api/scenes/{id}` 接口。启动脚本把 SQLite 数据放在本目录 `data/pascal.db`，不会覆盖其他 Pascal 安装的数据。迁移时请先停止服务，再备份整个 `data` 目录。

镜头工程按 sceneId 保存在当前浏览器的 localStorage，键为 `camera-studio:v1:{sceneId}`。使用 **导出镜头 JSON** 备份，换浏览器或换电脑时再导入。交付中的 `examples/camera-shots.json` 是与 `examples/empty-table.json` 对应的三机位工程。镜头 JSON 引用当前场景的物体 ID，因此迁移时须同时保留对应场景；清理浏览器站点数据会移除本地镜头缓存。

每个机位支持一条物体行动线，当前行动线控制物体位置。它适合道具移动、镜头路径和目标跟随；没有实现骨骼角色表演或从实拍视频反求三维动作。此版提供可编辑的镜头演绎工具和三个示范镜头，并不等于已自动完成《空桌》所有物件的整场编舞。

## 六层排演迁移（2026-09-09）

已完整读取上传的 `zhijiao-camera-studio-source.zip` 中 README 和四个源码文件，再接入此工作区已有实现。点击顶部 **场景序列** 进入六层面板；点击 **建模** 返回建模工具，点击 **镜头** 回到原镜头面板。原有建模 UI、米白场景、用户已保存的机位均保留。

1. 调整视口后点击 **设为 A**；换一个视角点击 **设为 B**。可从下拉框选择注视对象并点击 **转向注视对象**，将当前镜头转向对象。
2. 选择 **固定、导轨或环绕**，设置时长与缓动，再点击 **试走**。停止后可拖动时间条检查画面。
3. **镜头试拍录制器** 记录手动操机；点击开机后在三维视口移动镜头，停止时保存采样并可简化关键帧。
4. **影片渲染队列** 可输出 WebM 或 PNG ZIP。画面按输出比例居中裁切，安全框与裁切范围一致。导出期间可点击顶部 **停止当前操作**，也可直接返回建模。

六层包括摄像机滞后、摄像机绑定、序列编辑器、过场动画摄像机、镜头试拍录制器和影片渲染队列。新系统的“注视对象” 是一次转向，不会自动驱动物体行动线；持续跟随移动物体仍使用原有“镜头”面板。传感器高度与焦距控制垂直视场；传感器宽度、光圈及焦点距离保留为参数，目前没有景深虚化。

WebM 使用浏览器实时编码，性能不足会延长生成视频的时间。PNG 按固定镜头时间取样，单次上限 900 帧；这不是冻结全部场景仿真或 UE5 离线渲染。输出分辨率是画布尺寸，清晰度受当前三维视口实际渲染分辨率影响。两种输出均无配音。浏览器负责下载文件，界面成功提示不代表文件已保存到磁盘。

新镜头状态按 sceneId 独立保存在 `zhijiao.camera-sequence.v1:{sceneId}`；旧 `camera-studio:v1:{sceneId}` 不参与迁移覆盖。两个系统使用同一份场景，镜头参数各自保存，切换场景不会共享 A/B 或 Take。

实际源码落点：

- `apps/editor/lib/camera-director.ts` 和 `.test.ts`：六层数据、采样、缓存校验、场景隔离。
- `apps/editor/components/camera-rehearsal-panel.tsx`：Studio 控制面板。
- `apps/editor/components/camera-rehearsal-system.tsx` 和 `.test.ts`：R3F 内播放、录制、导出及退出恢复。
- `packages/editor/src/components/editor/index.tsx`：新增 `studioSceneSlot`，只挂在 Studio 的正常三维 Viewer 内；Edit 专属的 `viewerSceneSlot` 条件保持原样。
- `packages/editor/src/components/ui/sidebar/tab-bar.tsx`：可选工作区过滤，标签与面板一致。
- `apps/editor/app/page.tsx`、`components/scene-loader.tsx`、`components/camera-studio/dock.tsx`：接入两个场景入口和原控制栏。

Studio、Edit、第一人称、Capture、二维及 Preview 的挂载和旧镜头禁用边界有回归覆盖。新系统仅在透视视口接管相机，正交投影下禁用；进入和退出不强制改变原投影选择。离开 Studio 会同步恢复相机姿态、FOV、图层和控制参数，保存进行中的 Take，取消尚未完成的导出。预演、WebM 与 PNG 共用阻尼计算，回归测试确认相同步进下位置、注视点及 FOV 一致。

本次迁移架构复审：Blocker 0、Suggestion 0、Nit 0，可交付；已修复复审发现的轨迹差异和正交模式接管问题。

## 已验证

`启动镜场.ps1` 已实际成功启动本机服务；首次种子初始化时，已核对隔离数据库 `data/pascal.db` 中的 33 个节点及数据校验值与当时的 S00 样例一致。当前交付样例已更新为米白家居材质，33 个节点的位置保持不变。

在真实浏览器中验证了三个机位、连续预演与无声视频页面回放。一次 8 秒横屏录制的页面视频元数据为 **1280×720、7.9898 秒、MP4**。这证明页面中已生成可播放的视频；没有据此声称视频文件已经保存到用户磁盘。

此前镜头迁移的 72 项回归通过，应用和 editor 包独立 TypeScript 检查及生产构建通过。原有机位在浏览器中仍显示四个（包括用户新增机位）。新系统已在真实页面完成 A/B、Orbit、Take、WebM、PNG ZIP、导出中返回建模及二维停机检查。新导出的文件由浏览器发起下载，未声称已检查磁盘文件内容。迁移结束时场景版本为 2、节点数为 33，graphHash 与迁移前一致。原界面见 `previews/镜头编辑界面.jpg`。

随后完成中文界面与工作区导航调整，见 `中文界面说明.md`。本轮浏览器自动保存将场景规范化为版本 4，仅增加空的 `collections` 和原有插件列表 `installedPlugins`；33 个节点与材质数据逐项保持一致。未重置场景、旧机位缓存或六层排演缓存。

构建保留一条上游 SQLite 动态路径追踪提示，Biome 保留一条上游 effect 依赖提示，均未修改无关代码。

## 漫游鼠标锁定修复（2026-09-09）

修复第一人称入口、画面点击和 P 恢复操作中未捕获的鼠标锁定 Promise 拒绝。应用、Capture 和独立 GLB 漫游现在共用异常处理；Capture 的一次延迟重试仍保留，退出或画布移除后不再重试。

当前内置浏览器可能拒绝鼠标锁定，此时可按住左键拖动画面环顾，并按 Esc 退出；支持锁定的浏览器仍按原方式操控。该修复处理网站兼容性，不代表修改了 Chromium 内核。报错文字来源可见 [Chromium 鼠标锁定实现](https://chromium.googlesource.com/chromium/src/%2B/0a8c7ee1034e43691c810ab4dcc9b72eef8dc951%5E%21/)。

本轮 14 项相关回归通过，应用与 editor 独立类型检查、viewer 构建和生产构建通过。真实页面验证进入漫游、未锁定时拖动环顾、P 重试与 Esc 退出，浏览器错误日志为空。

## 来源与许可

Pascal 原始版权为 **Copyright (c) 2026 Pascal Group Inc.**，按 MIT License 提供。本目录保留原始 `LICENSE`；复制或再分发时须保留原版权及许可声明。官方许可文件：<https://github.com/pascalorg/editor/blob/main/LICENSE>。

这是一份开源自托管版，尚未部署到公网。保留完整开源源码，代表可继续编辑、运行和扩展这份本地代码。它不包含线上账号、私有服务、付费 AI 或云端功能的复刻；这些能力仍取决于相应服务、配置和授权。
